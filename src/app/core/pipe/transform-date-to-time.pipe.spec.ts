import { TransformDateToTimePipe } from './transform-date-to-time.pipe';

describe('TransformDateToTimePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformDateToTimePipe();
    expect(pipe).toBeTruthy();
  });
});
