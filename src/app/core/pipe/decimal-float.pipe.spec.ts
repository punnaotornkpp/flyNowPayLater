import { DecimalFloatPipe } from './decimal-float.pipe';

describe('DecimalFloatPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalFloatPipe();
    expect(pipe).toBeTruthy();
  });
});
